<h3>Job form</h3>

<div class="well">
  {{ form(form) }}

<!--
  {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

  {# Global errors. #}
  {{ form_errors(form) }}

  {# Generate label + error + widget for a field. #}
  {{ form_row(form.createdAt) }}

  {# Generate manually amd exploded: #}
  <div class="form-group">
    {# Génération du label. #}
    {{ form_label(form.title, "Title of the job advertisement", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

    {# Display errors for this field. #}
    {{ form_errors(form.title) }}

    <div class="col-sm-10">
      {# Generate input. #}
      {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
    </div>
  </div>

  {# Another field. #}
  <div class="form-group">
    {{ form_label(form.content, "Content of the job advertisement", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
    {{ form_errors(form.title) }}
    <div class="col-sm-10">
      {{ form_widget(form.content, {'attr': {'class': 'form-control'}}) }}
    </div>
  </div>

  {{ form_row(form.author) }}
  {{ form_row(form.isPublished) }}

  {# For the save button, no label no error, just the widget #}
  {{ form_widget(form.save, {'attr': {'class': 'btn btn-primary'}}) }}

  {# Automatic generation of fields not yet written.
     In this example, it would be the CSRF field (automatically managed by Symfony)
     and all hidden fields #}
  {{ form_rest(form) }}

  {{ form_end(form) }}
-->
</div>